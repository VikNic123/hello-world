    on:
      push:
        branches:
          - your-branch-name # Replace with the target branch name

    jobs:
      build:
        runs-on: [self-hosted, your-runner-label] # Replace with your runner's label
        steps:
          - uses: actions/checkout@v4
          - name: Build C project
            run: |
              # Commands to compile your C project
              # Example for a simple C project:
              gcc -o my_program main.c
              ./my_program

              
